:80 {
    encode zstd gzip

    @api path /api/* /health /docs* /openapi.json
    handle @api {
        reverse_proxy api:8080
    }

    handle {
        root * /srv/admin
        try_files {path} /index.html
        file_server
    }
}

